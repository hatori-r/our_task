.contents.row
  .container
    = form_with(model: @task, local: true) do |form|
      %h3
        Share task
      = form.text_field :name, placeholder: "name", required: ""
      = form.text_area :task, placeholder: "task" , rows: "10", required: ""
      = form.select :state, [['New', 'New'], ['Doing', 'Doing'], ['Completed!', 'Completed!'], ['Failure', 'Failure']], { selected: 'New', selected: 1, required: "" } 
      %label Task State
      .input-field
      %div
      = form.date_field :limit_date, required: ""
      = form.submit "SEND" 


-# %select{:name => "state"}
-#   - @status.each_with_index do |s,i|
-#   %option{:value => s}= s

    @status = ['New', 'Doing', 'Completed!', 'discontinued']