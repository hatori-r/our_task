-# %form{action: "/tasks/#{@task.id}", method: "POST"}
-#   %input{name: "_method", type: "hidden", value: "PUT"}/
-#   %input{name: "authenticity_token", type: "hidden", value: form_authenticity_token}/
-#   %div
-#     %label{for: "task"} Task Name
-#     %input#task{name: "task", required: "", type: "text", value: @task.task}/
-#   %div
-#     %select{name: "state"}
-#       - @status.each_with_index do |s,i|
-#       %option{value: s}= s
-#     %label Task State
-#   %div
-#     %input{name: "limit_date", required: "", type: "date"}/
-#     %label Task Limit Day
-#   %p
-#     %button{type: "submit"} update

-# - @task.each do |t|
-#   .tasks-table
-#     .task-user
-#       %ul
-#         %li
-#           %i.far.fa-user-circle
-#         %li
-#           %a(href="#")
-#             .user-name= t.name
-#         %li.details
-#           = link_to '…', "/tasks/#{t.id}/edit"
-#     .tasks-box
-#       .limit-box
-#         .limit-upper
-#           .limit LIMIT
-#         .limit-lower
-#           .limit-date= t.limit_date
-#       .task-box
-#         .task-upper
-#           .task TASK
-#           .task-data
-#             .task-data-inner= simple_format(t.task)
-#             .created-date= t.created_at.to_s(:datetime)
-#             .status-table
-#               %a(href="#")
-#                 .now-state= t.state
-#     .other-box
-#       %ul
-#         %li
-#           %a(href="#")
-#             %i.far.fa-thumbs-up
-#         %li
-#         = link_to task_path(t.id), method: :get, class: "fa-comment-dots" do
-#           = icon('far', 'fa-comment-dots')
-#       .adjustment-box
= @task.user.nickname
= @task.limit_date
= @task.task
= @task.state
= @task.created_at.to_s(:datetime)

.container
  - if current_user
    = form_with(model: [@task, @comment], local: true) do |f|
      = f.text_area :text, placeholder: "a comment", rows: "2"
      = f.submit "SEND"
  - else
    %p ※ Please log in or register to comment.
    = link_to "Sign up", new_user_registration_path, class: 'new', id: 'up'
    = link_to "Log in", new_user_session_path, class: 'log-in'

.comments
  %h4 ＜コメント一覧＞
  - if @comments
    - @comments.each do |comment|
      %p
        %strong
          = link_to comment.user.nickname, "/users/#{comment.user_id}"
          ：
        = comment.text